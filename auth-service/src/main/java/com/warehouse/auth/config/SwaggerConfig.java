package com.warehouse.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Removed duplicate OpenAPI bean definition to avoid conflict with OpenApiConfig
}
